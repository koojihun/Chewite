<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ✅ 실제 앱 테마 -->
    <style name="Theme.Chewite" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- ✅ 스플래시 전용 -->
    <style name="Theme.Chewite.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary_500</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>

        <!-- ✅ 스플래시 끝나면 이 테마로 자동 전환 -->
        <item name="postSplashScreenTheme">@style/Theme.Chewite</item>
    </style>
</resources>